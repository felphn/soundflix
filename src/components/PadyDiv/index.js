import styled from 'styled-components';

const PadyDiv = styled.div`
    color: var(--white);
    padding: 45px 0px;
    font-style: normal;
    font-weight: bold;
    font-size: 16px;
`;

export default PadyDiv;
